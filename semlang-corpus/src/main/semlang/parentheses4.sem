@Test([], "1")
function parenthesesForExpressionsInSeparateStatements(): Integer {
  // Separated by a tab
  getIncrementer()	(Integer."1")
}

function getIncrementer(): (Integer) -> Integer {
  Integer.plus|(_, Integer."1")
}
