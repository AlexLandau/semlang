@Test([], "8")
function doSomeStuff(): Integer {
  let a = Var(Integer."1")
  let doubleA = double|(a)
  doubleA()
  doubleA()
  doubleA()
  Var.get(a)
}

function double(x: &Var<Integer>): Void {
  let newValue = Integer.times(Integer."2", Var.get(x))
  Var.set(x, newValue)
}
