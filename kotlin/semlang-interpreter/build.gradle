dependencies {
  compile project(':kotlin:semlang-api')

  testCompile project(':kotlin:semlang-parser')
  testCompile project(':kotlin:semlang-test-utils')
}

tasks.test.inputs.dir('../../semlang-corpus/src/main/semlang')
