
function sayHello(out1: ~TextOut): ~TextOut {
  let out2: ~TextOut = TextOut.print(out1, Unicode.String."Hello, world!\n")
  // Trying to reuse out1 should fail
  let out3: ~TextOut = TextOut.print(out1, Unicode.String."Hello, world!\n")
  out3
}
