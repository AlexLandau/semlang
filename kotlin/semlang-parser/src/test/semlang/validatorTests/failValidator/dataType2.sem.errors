// Data.equals requires the parameter type passed in to be of type class Data
function dataEqualsWrapper<T>(): (T, T) -> Boolean {
  // Named function binding
  Data.equals<T>|(_, _)
  ~~~~~~~~~~~~~~~~~~~~~ Type parameter T requires a data type, but T is not a data type
}
