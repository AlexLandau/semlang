function myStuff.myFunction(): () -> Integer {
  let increment = Integer.plus|(_, Integer."1")
  // Too few arguments
  increment|()
  ~~~~~~~~~~~~ The function binding here expects 1 arguments, but 0 were given
}
