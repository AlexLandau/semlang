function notEnoughArguments(): Integer {
  let fn = Integer.plus|(Integer."3", Integer."4", Integer."5")
           ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ The function binding here expects 2 arguments, but 3 were given
  fn()
}
