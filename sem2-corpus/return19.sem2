@Test(["true", "true"], "3")
@Test(["true", "false"], "1")
@Test(["false", "true"], "4")
function conditionalEarlyReturn(b1: Boolean, b2: Boolean): Integer {
  let s: String = if (b1) {
    if (b2) {
      String."foo"
    } else {
      return Integer."1"
    }
  } else {
    return Integer."4"
  }
  List.size(s->codePoints).integer
}
