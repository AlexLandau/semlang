@Test(["2", "3"], "5")
@Test(["-10", "5"], "-5")
function addNaturalToInteger(i: Integer, n: Natural): Integer {
  // n should be replaced by n.integer in the generated sem1
  let foo = Integer.plus|(i, n)
  foo()
}
