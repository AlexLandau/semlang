
struct myStuff.Point<T> {
  x: T
  y: T
}

function myStuff.genericFunction<T>(input: myStuff.Point<T>): Integer {
  Integer."1"
}

@Test(["0"], "2")
@Test(["1"], "6")
@Test(["2"], "12")
@Test(["3"], "20")
function myStuff.myFunction(input: Natural2): Integer {
  let 1 = Natural2."1"
  let newX = Natural2.plus(input, 1)
  let newY = Natural2.plus(newX, 1)
  let object = myStuff.Point<Natural2>(newX, newY)
  let partialOutput = Natural2.times(object->x, object->y)
  let specificFunction = myStuff.genericFunction<Natural2>|(_)
  let constantFactor = specificFunction(object)
  let output = Integer.times(partialOutput->integer, constantFactor)
  output
}
