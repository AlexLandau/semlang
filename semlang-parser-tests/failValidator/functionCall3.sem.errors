function myStuff.myFunction(): Integer {
  let myInt = Integer."1"
  myInt(Integer."3")
  ~~~~~ The expression Variable(type=semlang:lang:0:Integer, aliasType=PossiblyAliased, name=myInt) is called like a function, but it has a non-function type semlang:lang:0:Integer
}
