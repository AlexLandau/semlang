
// Non-boolean requires block

struct Foo {
  a: Integer
  requires { Integer."1" }
           ~~~~~~~~~~~~~~~ Struct Foo has a requires block with inferred type semlang:lang:0:Integer, but the type should be Boolean
}
