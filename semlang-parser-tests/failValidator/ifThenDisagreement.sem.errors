// Both blocks of an if/then statement must return the same type
function myFunction(): Integer {
  if (Boolean."true") { Integer."1" } else { Boolean."false" }
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ Cannot reconcile types of 'then' block (semlang:lang:"fake0:0":Integer) and 'else' block (semlang:lang:"fake0:0":Boolean)
}
