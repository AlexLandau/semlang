function myStuff.myFunction(): Integer {
  let builderMaker = ListBuilder<_>|()
  // Rebinding with a type parameter of an unknown type
  let intListBuilderMaker = builderMaker<Inntegeer>|()
                                         ~~~~~~~~~ Type Inntegeer not found
  Integer."1"
}
