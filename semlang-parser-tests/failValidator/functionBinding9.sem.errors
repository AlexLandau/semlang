function myStuff.myFunction(): Integer {
  let increment = Integer.plus|(_, Integer."1")
  // Calling a binding with an argument of an incorrect type
  increment(Boolean."true")
  ~~~~~~~~~~~~~~~~~~~~~~~~~ The function expression expects arguments with types [semlang:lang:0:Integer], but was given arguments with types [semlang:lang:0:Boolean]
}
