
function doStuff(i: Integer): Integer {
                              ~~~~~~~ Stated return type Integer does not match the block's actual return type Natural
  // Explicit return type is incorrect
  let myFunction = function(a: Integer): Natural { Integer.plus(a, Integer."1") }
                   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ The inline function has a return type Natural, but the actual type returned is Integer
  myFunction(i)
}
