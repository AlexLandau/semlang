@Export
function Boolean.all(values: List<Boolean>): Boolean {
  List.reduce(values, Boolean."true", Boolean.and|(_, _))
}

@Export
function Boolean.any(values: List<Boolean>): Boolean {
  List.reduce(values, Boolean."false", Boolean.or|(_, _))
}
