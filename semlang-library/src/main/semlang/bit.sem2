@Export
struct Bit {
  natural: Natural
  requires {
    Boolean.or(
      natural == Natural."0",
      natural == Natural."1"
    )
  }
}

@Export
function Bit.equals(left: Bit, right: Bit): Boolean {
  left.natural == right.natural
}

@Export
struct BitsBigEndian {
  bits: List<Bit>
}
